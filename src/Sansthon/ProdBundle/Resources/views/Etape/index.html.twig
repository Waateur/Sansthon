{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Etape list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Initiale</th>
                <th>Final</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('etape_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.initiale }}</td>
                <td>{{ entity.final }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('etape_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('etape_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('etape_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
