{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Definitionrole creation</h1>

    <form action="{{ path('definitionrole_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('definitionrole') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
